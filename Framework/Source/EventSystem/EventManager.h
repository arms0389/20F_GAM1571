#pragma once

namespace fw {

class EventManager
{
public:
    EventManager();
    ~EventManager();

    //void AddEvent(Event* pEvent);
    //void DispatchAllEvents();

protected:
    //GameCore* m_pGameCore;

    //std::queue<Event*> m_Events;
};

} // namespace fw
